[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "node --max-old-space-size=1024 packages/backend/dist/index.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
